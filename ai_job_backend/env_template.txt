# Copy this file to .env and fill in your values
# Azure ML DeepSeek-R1 (hardcoded model, no OPENAI_MODEL needed)
OPENAI_API_KEY=your_api_key_here
# Azure AI Foundry v1: https://YOUR-RESOURCE.services.ai.azure.com/openai/v1
OPENAI_BASE_URL=https://YOUR-RESOURCE.services.ai.azure.com/openai/v1
# If Azure returns 400/401, set api-version (e.g. 2024-08-01-preview or from Foundry docs)
# OPENAI_API_VERSION=2024-08-01-preview

# Scraper Configuration
USE_SELENIUM=false
USE_PLAYWRIGHT=true
USE_STEALTH=true
SELENIUM_HEADLESS=true
# LinkedIn/Glassdoor - set ONE of these (or both; ScraperAPI tried first):
# Option A: ScraperAPI (JS rendering, no browser) - https://www.scraperapi.com
# SCRAPER_API_KEY=your_scraperapi_key
# Option B: Browserless (free tier ~6 hrs/mo) - https://browserless.io
# BROWSERLESS_URL=wss://chrome.browserless.io?token=YOUR_TOKEN

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/job_assistant.log

# API Configuration
API_TIMEOUT=30

# Server Configuration
PORT=8000
HOST=0.0.0.0
FRONTEND_URL=http://localhost:3000
# For Vercel: set ALLOWED_ORIGINS to your Vercel URL(s), comma-separated for multiple (e.g. production + preview)
# ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-app-git-xxx.vercel.app

# Database Configuration
# Choose one: SQLite (easier), DATABASE_URL (Render/Heroku), or PG_* (PostgreSQL)
#
# Option 1: SQLite (No installation needed - set USE_SQLITE=true)
# USE_SQLITE=true
#
# Option 2: DATABASE_URL (Render/Heroku - paste from your PostgreSQL add-on)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
#
# Option 3: PostgreSQL via individual vars
USE_SQLITE=false
PG_HOST=localhost
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=your_postgres_password_here
PG_DATABASE=ai_job_assistant

# Supabase (required for LLM/DeepSeek R1 to fetch user saved preferences)
# Used by: /api/job/rank-for-user, agent get_user_profile tool.
# Uncomment and set in .env so profile_lookup can read profiles + user_preferences from Supabase.
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Auth Configuration
AUTH_SECRET_KEY=change-me-in-prod-secret-key-here
AUTH_ALGORITHM=HS256
